说明：
   批发商根据客户的级别和数量来绝对批发的价格
   新客户小批量：98折
   新客户大批量：9折
   老客户小批量：88折
   老客户大批量：8折
 
 
策略模式：
   将变化的抽取出来定义为接口或抽象类，不要用继承的方式         
环境(Context)角色：持有一个Strategy的引用。
抽象策略(Strategy)角色：这是一个抽象角色，通常由一个接口或抽象类实现。此角色给出所有的具体策略类所需的接口。
具体策略(ConcreteStrategy)角色：包装了相关的算法或行为。